<?php


    define('DB_HOST','localhost');
    
    define('DB_USUARIO','root');
    
    define('DB_CONTRA','');
    
    define('DB_NOMBRE','pruebas');
    
    define('DB_CHARSET','utf8');
    /*
    ------The define()-------
    function defines a constant. Constants are much like variables, except for the following differences: ... Constant names do not need a leading dollar sign ($) Constants can be accessed regardless of scope. CThe value of the constant. In PHP 5, value must be a scalar value (int, float, string, bool, or null). In PHP 7, array values are also accepted
    */

?>